from qiskit import QuantumRegister, ClassicalRegister, QuantumCircuit
from qiskit.primitives import Sampler
from qiskit.circuit.library.standard_gates import *
from qiskit.visualization import plot_histogram
from qiskit.quantum_info.operators import Operator
from math import pi
import numpy 
from qiskit.visualization import plot_state_city 
from qiskit.visualization import plot_histogram
from qiskit.circuit.library import MCMT


#numpy.set_printoptions(threshold=sys.maxsize)

qreg = QuantumRegister(#QUBITS#, 'q') 
creg = ClassicalRegister(#OUTPUT_QUBITS#, 'c')

circuit = QuantumCircuit(qreg, creg)

ONE = [0, 1]
ZERO = [1, 0]
#INITIALIZE#

#CALCULUS#

#MEASURES#


sampler = Sampler()
job = sampler.run(circuit, shots=#SHOTS#) 
result = job.result()

counts = result.quasi_dists[0].binary_probabilities()
absolute_frequencies = {outcome: int(probability * #SHOTS#) for outcome, probability in counts.items()}
output = ','.join([f"({int(outcome, 2)}:{frequency})" for outcome, frequency in absolute_frequencies.items()])
print(output)

#COUNTERS#
